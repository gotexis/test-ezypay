ezypay
===============
exis

To run api:

```bash
cd api && npm i && npm run dev
```

To test api (please do run them)

```bash
cd api && npm run test
```

To run frontend:

```bash
cd web && npm i && npm run serve
```
